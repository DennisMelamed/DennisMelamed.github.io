[project]
name = "typst-resume"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "typst>=0.11.1",
    "ubelt>=1.3.6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
